<UICore:BasePage x:TypeArguments="Core:QuestionMultipleChoiceViewModel" x:Class="Testinator.Client.QuestionMultipleChoicePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Testinator.Client"
             xmlns:Core="clr-namespace:Testinator.Client.Core;assembly=Testinator.Client.Core"
             xmlns:UICore="clr-namespace:Testinator.UICore;assembly=Testinator.UICore"
             mc:Ignorable="d">
    
    <Grid Background="{StaticResource WhiteLightBrush}"
          VerticalAlignment="Center"
          HorizontalAlignment="Center">

        <Border Background="{StaticResource WhiteDarkBrush}" 
                CornerRadius="3"
                Padding="15 50 15 15"
                Width="600"
                Margin="50 50 50 0">

            <StackPanel>

                <TextBlock Text="{Binding Task}" />
                <local:MultipleChoiceAnswersList DataContext="{Binding Instance}">
                    
                </local:MultipleChoiceAnswersList>

            </StackPanel>
            
            
        </Border>

    </Grid>
    
</UICore:BasePage>
